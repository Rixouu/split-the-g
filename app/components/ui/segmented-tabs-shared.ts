/**
 * Shared chrome for segmented tabs (add `flex` or `grid` on the container).
 * Kept in UI layer to avoid cross-importing route modules from components.
 */
export const segmentedTabGroupChromeClass =
  "w-full gap-1 rounded-xl border border-solid border-guinness-frame bg-guinness-brown/25 p-1 sm:p-1.5";
